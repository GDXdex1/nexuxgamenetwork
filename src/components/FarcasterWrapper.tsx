'use client';

import { ReactNode } from 'react';

export default function FarcasterWrapper({ children }: { children: ReactNode }) {
    // TODO: Add full Farcaster SDK initialization logic here
    return <>{children}</>;
}
